import urllib.request
import json
import re

def get_company_facts(company_name: str) -> str:
    """
    Stub: returns empty string until real search API (DuckDuckGo/Crunchbase) is integrated.
    Previously returned a fake hardcoded string that appeared in every pitch — removed.
    """
    return ""
